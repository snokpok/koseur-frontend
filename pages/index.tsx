import ChatSidebar from "../components/ChatSidebar/ChatSidebar";

export default function MainWorkspace() {
    return (
        <div>
            Main workspace
            <ChatSidebar />
        </div>
    );
}
